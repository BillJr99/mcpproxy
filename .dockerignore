# Secrets — never send to build context
.env
.env.*
!.env.example

# Python artifacts
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
*.egg-info/
.pytest_cache/

# Dev/VCS
.git/
.github/
*.md

# Volume bind-mount targets — never copy into the build context
files/
repos/
.playwright-mcp/
